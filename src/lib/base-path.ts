/**
 * Where the product lives: hyphy-studio.com/platform proxies to this deployment, so every page,
 * asset and action is served under this prefix. `next/link`, `redirect` and `usePathname` apply it
 * for you; only raw URLs (the manifest, tests) need it written in.
 */
export const BASE_PATH = '/platform';
